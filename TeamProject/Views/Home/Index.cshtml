
@model TeamProject.Models.PostsViewModel
<style>
    div {
        color: black;
    }

    .row {
        margin: 10px;
        font-size: 18px;
    }

    footer {
        text-align: center;
        font-size: 16px;
    }

    .posted {
        font-size: 16px;
    }

    .title {
        font-weight: bold;
    }

    hr {
        display: block;
        height: 1px;
        border-top: 1px solid #000000;
        margin: 1em 0;
        padding: 0;
    }
	.body-content-post {
		word-wrap: break-word;
	}

</style>
<div class="container">
        <div class="col-xs-5">
            @foreach (var post in Model.Posts)
                {
                <div class="panel panel-default">
                    <div class="panel-body">
                        <section class="post-heading">
                            <div class="row">
                                <div class="col-md-11">
                                    <div class="media">
                                        <div class="media-left">
                                            @{
                                                var imagePostUrl = Url.Content("~/Content/images/profile/") + post.Username + ".png" + "?time=" + DateTime.Now.ToString();
                                                
                                            <img class="media-object photo-profile" src="@imagePostUrl" width="40" height="40" onerror="this.src = '../Content/images/profile/default.png';" />
                                            }

                                        </div>
                                        <div class="media-body">
                                            <a href="#" class="anchor-username"><h4 class="media-heading">@post.Author</h4></a>
                                            <a href="#" class="anchor-time">@post.PostDate</a>
                                            <a href="#" class="anchor-title"><h3 class="media-title">@post.Title</h3></a>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-1 post-details-@post.Id">
                                    
                                    <a href='@Url.Action("PostById","Home",
                                           
                                            new {id = post.Id})'><i class="glyphicon glyphicon-chevron-down"></i></a>

                                </div>
                            </div>
                        </section>

                        <section class="post-body">
                            <div class="body-content-post">
                                @post.Body
                            </div>
                            
                        </section>
                        <section class="post-footer">
                            <hr />
                            <div class="post-footer-option container">
                                <ul class="list-unstyled">
                                    <li><a href="#"> <i class="glyphicon glyphicon-thumbs-up"></i> Like</a></li>
                                    <li><a href="#"><i class="glyphicon glyphicon-comment"></i> Comment</a></li>
                                    <li><a href="#"><i class="glyphicon glyphicon-share-alt"></i> Share</a></li>
                                </ul>
                            </div>
                              <div class="post-footer-comment-wrapper">
                                    @foreach (var comment in post.Comments)
                                    {

                                        <div class="comment-form">
                                            @comment.Text
                                        </div>
                                        <div class="comment">

                                            <div class="media">
                                                <div class="media-left">
                                                    @{
                                                        var imageUrl = Url.Content("~/Content/images/profile/") + comment.Username + ".png" + "?time=" + DateTime.Now.ToString();
                                                            <a href="#">
    
                                                                <img class="media-object photo-profile" src="@imageUrl" width="32" height="32" onerror="this.src = '../Content/images/profile/default.png';" />
                                                        </a>
                                                    }

                                                </div>
                                                <div class="media-body">
                                                    <a href="#" class="anchor-username"><h4 class="media-heading">@comment.Author</h4></a>

                                                    <a href="#" class="anchor-time">@comment.CommentPostDateDate</a>
                                                </div>
                                            </div>
                                        </div>
                                    }

                                </div>
                        </section>
                    </div>
                </div>
            }
        </div>
    </div>
@*<div class="jumbotron vertical-center">
        <div class="col-lg-12 small">
            @foreach (var post in Model.Posts)
            {
                <h2>@post.Title</h2>
                <div class="body-text">
                    <p class="text-muted">@post.Author | @post.PostDate</p>
                    <br/>
                </div>
                <div class="row">
                    <div class="col-lg-6">
                        <p>@post.Body</p>
                    </div>
                    @#1#if (post.Images != null)
                    {
                        <div class="col-lg-6"><p class=""><img alt="image" src="@post.images.path" width="100%"></p></div>
                    }#1#

                </div>
            }

        </div>
    </div>*@

@*@foreach (var post in Model.Posts)
{
    <div class="post-box text-center center-block">

        <h2>@post.Title</h2>
        <div class="post-body">
            @post.Body
        </div>
        <hr />
        <p class="posted">
            Posted on @post.PostDate
        </p>

        @if (post.Modified != null)
        {
            <p>Modified @post.Modified</p>
        }
        <hr />
        <div class="author">Author: @post.Author</div>
        @if (post.Comments != null)
        {
            <div class="comment">
                <h3>Comments: </h3>
                @foreach (var comment in post.Comments)
                {
                    <div class="comment-text" style="color: blueviolet">
                        @comment.Text
                    </div>
                            <div class="comment-author">
                                @comment.Author
                            </div>
                                <div class="comment-postData" style="color: brown">
                                    @comment.CommentPostDateDate
                                </div>
                }
            </div>
        }
    </div>

}*@
